/*javascript using two different data types
    


      1.primitive data type
      2.non-primitive data type

      1.primitive=srring,number,boolean,null,symbol,big int
      2.non-primitive =array,obbject data set*/

  //string(using  dowle cotetion and anywher accept)

  let movie="avager endgame"
  let number="1,3,4,5"
  console.log(movie,number)

  //number(use to diclare a number values only,and using operations )

  let a=10
  let b=1999
  console.log(a+b)
  
  //boolean(usinng only two value true are flase can use condision statement)
   
  let user=true
  let userr=false
  console.log(user)

  //undifined(its variyable values not assigned its worked )
  let allpha;
  console.log(allpha)


  //null(its means nothing its created memory location but valued not)
   // let nulldt=null
    //console,log(nulldt)
  //symbol(its alloso normal string but add symboil keyword too show)
    let lan=Symbol("aadhi")
    console.log(lan)


    //big int(number datat type alternate the method because the num data only using15 digits after the methed not wprked so its using)

     let d=162627828636883738917638n
     console.log(typeof(d))


 //non-primitive datatypes
 //array(its using add number strinf bollian all data types accepct its using square brakets)
 let list=[1,2,3,4,'aadhi,ff,',true]   
 console.log(list) 


 //object (its allso accepect all datat types currlly braces and use key names)
 let all={
    brand:'lenova',
    Model:123
 }
console.log(typeof(all))
//date (can use date see)
let cdate=new Date()
console.log(cdate)


//you can see the data type write (typeof(v.name)) to see the datat type




// Numbers:
let length = 16;
let weight = 7.5;

// Strings:
let color = "Yellow";
let lastName = "Johnson";

// Booleans
let x = true;
let y = false;

// Object:
const person = { firstName: "John", lastName: "Doe" };

// Array object:
const cars = ["Saab", "Volvo", "BMW"];

// Date object:
const date = new Date("2022-03-25");















